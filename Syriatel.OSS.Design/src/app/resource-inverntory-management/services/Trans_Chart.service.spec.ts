/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Trans_ChartService } from './Trans_Chart.service';

describe('Service: Trans_Chart', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Trans_ChartService]
    });
  });

  it('should ...', inject([Trans_ChartService], (service: Trans_ChartService) => {
    expect(service).toBeTruthy();
  }));
});
